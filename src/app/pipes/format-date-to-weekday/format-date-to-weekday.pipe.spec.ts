import { FormatDateToWeekdayPipe } from './format-date-to-weekday.pipe';

describe('FormatDateToWeekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateToWeekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
